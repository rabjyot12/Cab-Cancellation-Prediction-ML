# Trained Model

The trained `random_forest_model.pkl` is not included in this repository because its size (~250 MB) exceeds GitHub's recommended file size limits.

To regenerate the model:

1. Open `notebooks/01_Model_Training.ipynb`
2. Run all cells
3. The trained model will be saved automatically in the `models/` directory.
